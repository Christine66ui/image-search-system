[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python backend/app.py"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[env]
PORT = "5000"
PYTHON_VERSION = "3.10"
