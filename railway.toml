[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python backend/app.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
startTimeout = 60

[env]
PORT = "5000"
PYTHON_VERSION = "3.10"
